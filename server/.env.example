# Port for the Express server to listen on (Render will set this automatically)
PORT=5000

# The URL of your deployed frontend client (e.g. Vercel deployment)
# Used to secure CORS configuration
CLIENT_URL=https://personal-task-manager.vercel.app

# Environment setting (production, development)
NODE_ENV=production
